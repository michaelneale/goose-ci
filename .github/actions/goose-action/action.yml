---
name: "Goose AI Action"
description: "Sets up Goose AI with pipx"
inputs:
  profile:
    description: 'Profile configuration for Goose AI'
    required: true
  task_request:
    description: 'Task request for Goose AI'
    required: false

runs:
  using: "docker"
  image: "Dockerfile"
